'use strict';


// the perpuse of Strict mode
let hasDriversLicence=false;
const passTest=true;

if(passTest) hasDriersLicence=true;// error that only strict mode will detect it(if we write hasDriersLicence!=hasDriversLicence)
console.log(hasDriversLicence?"I can drive":"i cant Drive :(");

// strict mode can preserve some words that can be used in future fichers like
const interface='Audio'; 
const private =534;
const x= 500;