// there are 3 sorces of data
// 1) the source code it self (status messages ...)
// 2)from the UI (from , users tasks in a list ...)
// 3)external sources (APIs)

// if we need just a simple list of values
// array :
    // contain duplicate data 
    // we manipulate this data a lot
// set :
    // we dont want duplicate values
    // High performance is important (operations like searching for an item are 10 time faster than in arrays )
    // delete duplicate values from an array
// if we need a key/value pairs 
// object
    // if we need methods inside
    // when working with json data 
    // easier to write 
//  map
    // better performance
    // key can have any type
    // easy to iterate(iterable)
    // Esy to compute size(size attribute)